# Documentação de Machine Learning (placeholder)

> Placeholder criado automaticamente para satisfazer links internos (referenciado em `docs/guides/architecture.md` e `docs/guides/development-guide.md`).

Adicione aqui detalhes sobre os modelos, pipelines de treinamento, como executar testes de inferência e onde os modelos são armazenados (`ml_models/`).

- Local dos modelos: `ml_models/`
- Como treinar: scripts em `ml/` (ex: `train.py`)
- Como validar: métricas, datasets de validação

Substitua este arquivo pelo conteúdo real da documentação ML.
