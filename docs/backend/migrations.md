# Migrações de Banco de Dados (Alembic)

> Placeholder criado automaticamente para satisfazer links internos (referenciado em `docs/backend/models.md`).

Inclua aqui instruções sobre como gerar e aplicar migrations com Alembic:

- Como criar uma migration: `alembic revision --autogenerate -m "mensagem"`
- Como aplicar: `alembic upgrade head`
- Como reverter: `alembic downgrade -1`

Substitua este arquivo pelo guia completo de migrations quando disponível.
